# 02 Base 11.par Model 02

Independent model folder using the BET 2026 `11.par` input. Replace this
folder's files or add `patch.R` to turn it into a real model variant.

The root `stepwise-config.R` controls whether this model runs and which input
and output file names are used.
